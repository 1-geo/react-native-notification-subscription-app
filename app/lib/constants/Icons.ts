import {Leaves} from '../../definitions';

export const Icons = {
  loading: require('../../assets/loading.png'),
  search: require('../../assets/search.png'),
};

export type IIcons = Leaves<typeof Icons>;
