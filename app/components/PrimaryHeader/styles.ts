import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  header: {
    fontSize: 26,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});

export default styles;
